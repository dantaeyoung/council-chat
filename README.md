Deploy to Firebase from Glitch
=================

A simple starter project for deploying sites to Firebase Hosting. Deploy to Firebase by opening the `firebase.json` file and clicking the `Deploy to Firebase` button.
